# Use for token generation (You can use `openssl rand -hex 32`)
SECRET_KEY = "change_me_in_production"

# JWT Configuration (You can change it if you want)
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 30

# Database
DATABASE_URL = "change_me_in_production"

# Path to files
SCHEDULES_PATH = "change_me_in_production"
RESOURCES_PATH = "change_me_in_production"
